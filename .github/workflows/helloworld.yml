name: Hello World

on:
  workflow_dispatch:

jobs:
  test-hello-world:
    runs-on: node:slim
    steps:
      - name: install
        run: npm install typescript @types/google-apps-script jest ts-node ts-jest @types/jest
      - name: test
        run: npx jest
